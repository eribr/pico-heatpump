# Configuration settings for the Nibe heat pump project

# Note: Nibe-specific constants (POWER_ON/OFF, MODE_*, FAN_*, VDIR_*, etc.)
# are defined in models/nibe.py to keep model-specific code together.

# IR hardware configuration
# Set to the pin number (e.g. 0) or pin name (e.g. "LED") used for the IR transmitter.
IR_PIN = "LED"